[user]
	name = Ryan Frankel
	email = ryan@digitalbrands.com
[filter "lfs"]
	clean = git-lfs clean -- %f
	smudge = git-lfs smudge -- %f
	required = true
[credential]
	helper = cache --timeout=86400
[color]
  ui = auto

;[color "branch"]
  ;current = yellow reverse
  ;local = yellow
  ;remote = yellow
;[color "diff"]
  ;meta = yellow bold
  ;frag = magenta bold
  ;old = yellow bold
  ;new = green bold
;[color "status"]
  ;added = yellow
  ;changed = green
  ;untracked = cyan
